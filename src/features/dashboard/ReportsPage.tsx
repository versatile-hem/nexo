import { Card } from "@/components/ui/card";

export function ReportsPage() {
  return (
    <Card>
      <h2 className="text-lg font-semibold">Reports</h2>
      <p className="mt-2 text-sm opacity-70">Export-ready analytics and scheduled reports can be plugged in here.</p>
    </Card>
  );
}
